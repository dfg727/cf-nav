{
  "name": "nav-ui",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev:css": "tailwindcss -i ./src/input.css -o ./public/style.css --watch",
    "build": "tailwindcss -i ./src/input.css -o ./public/style.css --minify",
    "dev": "npm run build && wrangler pages dev public --port 8788",
    "deploy": "npm run build && wrangler pages deploy public --project-name=nav-ui"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13",
    "wrangler": "^4.68.1"
  }
}
